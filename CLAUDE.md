# Japan Medicine Guide

豪州からの訪日旅行者向け、日本の市販薬を英語で読むためのガイド。
Next.js (App Router) / Supabase / Vercel / TypeScript。

読者は Perth や Sydney から来た旅行者。Panadol と Nurofen を常備し、
Pharmacist Only (S3) という概念を知っている。日本語は全く読めない。
迷ったら「Perthから来た人が棚の前で3秒で使えるか」で判断する。

このプロジェクトはユーザーテストを行わない。
一般的な UX 原則、アクセシビリティ、モバイル利用、医療情報サービスとしての
信頼性を根拠に判断する。ユーザーへの確認待ちで作業を止めない。

## v2 について（2026-08-27 に方針転換）

v1 は検索窓を主役にしたため使いにくかった。
姉妹プロダクトの豪州版（australia-okusuri-guide）の構造に寄せて作り直す。
データと設計判断は全て残る。作り直すのは画面のコードのみ。

### 廃止したもの。復活させないこと

- 検索窓、オートコンプリート、`/find?brand=`
- `/phrases`（25件の縦一覧）→ `/consult` の組み立て式に作り替えた
- 「同系統・別成分」（same_class）の表示
- IBM Plex 系の書体と #FBFAF7 系の配色
- 商品レコメンド、AIチャット、ログイン、会員機能、フォーム送信
- 広告、課金、アフィリエイト

## 絶対に守ること

- 事実の提示のみ。「どれを飲むべきか」は書かない
- 成分が完全一致するものだけ対応付ける。「同系統・別成分」は出さない
- 用語は豪州英語。paracetamol と書く（acetaminophen とは書かない）。綴りは -ise 系
- 「drugstore」ではなく「chemist」
- 赤（--stop #B4462F）は「そのまま進めない」合図にのみ使う（第1類・受診勧奨）
- 成分の日本語名は DB に持つが UI には出さない（監修者のレビュー専用）
- `/consult` は商品データと絶対に繋がない（症状→商品はレコメンドになる）
- 商品詳細に必ず戻る導線を置く。行き止まりを作らない
- slug は変更しない。主キーであり URL であり、foreign_brands の参照先でもある

## 表示しないもの（意図的な省略。追加しないこと）

- 成分の含有量（用量の比較は医療判断になる）
- 成分の薬効分類
- パッケージ外観の記述（添付文書に載っておらず検証不能）
- 商品パッケージ画像（著作権・商標）
- 商品ごとの年齢・妊娠の警告
  → 全商品共通の固定文に統合:
  "Ask the pharmacist if you are pregnant, taking other medicine, or under 15."
- カテゴリ一覧での成分の羅列（一覧に並べると成分比較表になり、選択の助言に近づく）
- 薬剤師の在店時間（店舗差があり検証不能。「夕方以降は不在の店が多い」で止める）
- 症状のピクトグラム（症状チェッカー・診断サービスに見える）

検証が必要な事実は1商品あたり3つ（商品名／成分名／区分）に抑える。

## 画面構成（5画面）

1. `/` カテゴリ一覧 + タブバー
2. `/category/[slug]` 商品一覧。並び順を設計する
3. `/products/[slug]` 商品詳細。棚と照合する画面 + 店員向け表示
4. `/consult` 3ステップで日本語カードを組み立てる
5. `/about` Disclaimer・監修体制

`/review` は監修用に残す。公開導線には出さない。sitemap にも含めない。

## ナビゲーション：タブバー

```
[ Medicines ]  [ Ask pharmacist ]
```

**設置するのはトップ・商品詳細・consult の3画面。カテゴリ画面には置かない。**

| 画面               | タブ     | 理由                                                                                                                                                                              |
| ------------------ | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `/`                | あり     | 8カテゴリを並べると `Ask pharmacist` が iPhone SE の折りたたみより下になる。2つの機能を折りたたみの上に出すためにタブが要る                                                       |
| `/category/[slug]` | **なし** | 商品は最大6件で、末尾の逃げ道が画面内に入る。タブを置くと同じ機能への導線が1画面に2つになる。末尾の導線のほうが「棚で見つからなかった」という詰まった瞬間に置かれていて優れている |
| `/products/[slug]` | あり     | 英語の情報と日本語の提示を切り替える（下記）                                                                                                                                      |
| `/consult`         | あり     | Medicines に戻れる                                                                                                                                                                |
| 完成カード         | なし     | 薬剤師に見せる画面。タブもフッターも消し、背景を白に切り替える                                                                                                                    |

- 選択中の側はタップ不可。`aria-current="page"` を付ける
- 選択状態は色だけでなく塗り＋太字で示す
- タップ領域は 44px 以上

### 商品詳細のタブだけ挙動が違う

トップと consult では `Ask pharmacist` は `/consult` へのリンク。
**商品詳細では遷移せず、同じページ内で店員向け表示に切り替わる。**

商品詳細まで来た利用者が `Ask pharmacist` に期待するのは
「この商品について聞く手伝い」であり、症状のカード作成ではない。
一貫性より、その場面での期待に応えることを優先した。
試作を2案（placard の全画面表示／タブでのページ内切り替え）作り、
実機で比較したうえでの判断。

商品詳細から `/consult` へ行きたい場合はフッターの `Make a card` を使う。

### /consult の状態保持

**前提条件：`/consult` の回答を `sessionStorage` に保持すること。**
これがないと Medicines を押した瞬間に入力が消え、
常設ナビが「押すと怖いもの」になる。
`sessionStorage` はタブを閉じれば消えるため、
「端末内のみで保持・保存も送信もしない」という方針と矛盾しない。

## トップページの原則

説明文で理解させない。ナビゲーション自体が使い方を説明する構造にする。
画面の主役はサービス名ではなく、問いかけと直下の2択。

```
JAPAN MEDICINE GUIDE
AUSTRALIA → JAPAN
At a chemist in Japan?          ← ここまでブランドヘッダー（--brand の塗り）

[ Medicines ]  [ Ask pharmacist ]

MATCH THE NAME ON THE BOX
Pain & Fever / 痛み・熱
...
```

- ヒーローエリアを大きくしない。ブランド表現より操作開始の速さを優先する
- 新しい導入画面、モーダル、カルーセルを追加しない
- 説明文をこれ以上増やさない

## 並び順は設計項目

products.sort_order の昇順で表示する。DBの返す順に任せない。
原則：豪州人にとって役立つ順。

1. 豪州ブランドと同成分のもの（Panadol → タイレノールA）
2. その他
3. 第1類（夜は買えないため最後）

sort_order = 9 は第1類など「そのまま進めない」商品に予約。
1〜5 が通常商品、6〜8 は予約枠。

カテゴリ自体の並び順の根拠は未確定。公開後のタップデータで決め、根拠ごと記録する。

## 外国ブランドの扱い

foreign_brands は入口ではなくラベルとして使う。

- カテゴリ一覧のカードに "Same as Panadol"
- 商品詳細に "Same active ingredient as ..." として1行
  （"Same ingredient as" ではない）
- `foreign_brands.caveat_en` がある場合は表示する

成分が完全一致するものだけ。別成分を「相当する」と書かない。

「Same as X」は**有効成分が1つだけの商品にのみ**表示する。
配合薬に表示すると置き換え可能だと読まれるため。
現在ラベルが出るのは6件:
タイレノールA / アレグラFX / ガスター10 / クラリチンEX / ストナリニZ / ボルタレンEXテープ

**対応がないことを理由に "Not sold in Australia." と断定しない。**
対応がない商品では該当の表示ブロックごと出さない。

登録ブランド（豪州のみ・8件）:
Panadol→paracetamol / Nurofen→ibuprofen / Aspro→aspirin /
Pepcid→famotidine / Telfast→fexofenadine / Claratyne→loratadine /
Zyrtec→cetirizine / Voltaren→diclofenac

## 商品詳細（/products/[slug]）

### Medicines 側の表示順

```
placard（MATCH THIS ON THE SHELF / name_ja 34px / name_romaji）
summary_en
第1類の警告（該当商品のみ）
ACTIVE INGREDIENT
Same active ingredient as ...（該当商品のみ）
固定文（Ask the pharmacist if you are pregnant...）
View manufacturer source ↗ / Checked YYYY-MM-DD
‹ Back to {category}
```

**Medicines 側は上から下まで英語。** 日本語が出るのは商品名 placard だけで、
これは「棚と照合する対象」なので例外になる。
英語の画面に日本語のブロックが挟まると、日本語が読めない利用者にとって
視覚的なノイズになる。

### Ask pharmacist 側（店員向け表示）

タブを切り替えると、この画面だけが表示される。

```
placard（帯 店員の方へ / 日本語 28px）
  {name_ja} を探しています。
  どこにありますか？

This asks a staff member where to find it.   ← 11.5px / --ink-2
```

- 中身は `name_ja` から組み立てる。新しい事実を増やさないので監修不要
- 英語の補足は「見せてください」ではなく**何が書いてあるか**を伝える。
  完成カードの WHAT IT SAYS と同じ考え方
- 英語は日本語より視覚的に弱くする。この画面の主役は店員が読む日本語

## /consult（3ステップ）

1/3 症状（複数選択・日常語のみ）
2/3 期間（単一選択）
3/3 状況・希望（複数選択・全スキップ可）
→ 完成カード

- Step 1 の症状は部位でグルーピングする（NOSE & THROAT / TEMPERATURE /
  HEAD & BODY / STOMACH / SKIN & EYES）。フラットに14個並べない。
  グループ名は DB に持たせず、コード側の定数で管理する
- Fever と No fever は排他ペアにする（複数選択のまま両方押せるのは矛盾）
- Step 2 は未選択の間 Next を disabled にする。disabled を色だけで示さない
- Step 3 は何も選ばず進めることが明確に分かるようにする
- 3ステップで完了することを最初から示す（プログレスバー + "STEP 1 OF 3"）
- 完成カードは日本語が主役（17px前後）。英語は下に小さく「何と書いてあるか」を添える
- 最後の1文は必ず入れる:
  「日本語が分かりません。お答えを書くか、商品を見せてください。」
  豪州版と非対称にする。利用者は日本語が全く読めないため「書く」だけでは足りない
- 受診勧奨の赤枠は選択内容に関わらず常時表示の固定文。条件分岐にしない
- 選択内容は `sessionStorage` のみで保持。保存も送信もしない
- 完成カードではタブバーもフッターも非表示にし、背景を白に切り替える
- 完成カードの Full screen タップで `navigator.wakeLock.request('screen')` を呼ぶ。
  ユーザージェスチャ内なので許可される。非対応環境では何も起きないためフォールバック不要。
  閉じるときに release すること
- `Start over` は破壊的操作に見えない控えめなスタイルにする。
  `sessionStorage` を明示的にクリアすること

## データベース（Supabase・すべて slug が主キー）

商品32点 / 8カテゴリ。全件監修済み（2026-08-29 時点）。

```
pain-fever(5) / cold-flu(5) / stomach(6) / allergy(4) /
skin(5) / eye-drops(3) / motion-sickness(2) / muscle-joint(2)
```

```
products(slug, name_ja, name_romaji, summary_en, maker, otc_class,
  category, form, search_terms text[], sort_order, source_url,
  verified, reviewed_at, updated_at)
ingredients(slug, name_en, name_ja, note_en, verified)
product_ingredients(product_slug, ingredient_slug, sort_order)
foreign_brands(name, ingredient_slug, caveat_en, source_url, verified)
categories(slug, name_en, name_ja, sort_order)
consult_options(slug, step, text_en, text_en_full, text_ja, sort_order)
```

otc_class は enum: 'class1' | 'designated_class2' | 'class2' | 'class3'
→ UI では designated_class2 と class2 を同じ表示に統合する
→ **表示文言は DB に持たず lib/otcClass.ts の定数で一元管理する。
JSX に直書きしない**

consult_options.step: 1=症状 / 2=期間 / 3=状況
`text_en` はチップのラベル、`text_en_full` は完成カードの WHAT IT SAYS 用。
両者が同一になる行があるのは正常。列を分けた目的は重複回避ではなく、
独立して変更できるようにすること。

成分は product_ingredients.sort_order の順で表示する（箱の印刷順）。

`ingredients.note_en` は豪州で OTC 入手できない成分の注記に使う
（コデイン類、プソイドエフェドリン、外用ステロイド）。
成分マスタに1行書けば該当する全商品に反映される。商品ごとに事実を増やさない。

search_terms は v2 では使わない。列は残すが参照しない。

**スキーマ変更と既存データの変更は Claude Code では実行しない。**
SQL が必要な場合は apply / verify の2本のファイルを生成し、
私が Supabase の SQL Editor で流す。apply は begin/commit で囲む。
インラインコメントを SQL 文の途中に入れない（SQL Editor は選択範囲だけを
実行するため）。`reviewed_at = now()` は必ず最後に実行する。
ファイル名は `supabase/YYYY-MM-DD_内容-apply.sql` のように日付を先頭に置く。

`.env.local` には anon キーのみを置く。service_role キーを置かない。
DB は監修済みの成果物の置き場であり、読み取り専用の経路しか持たせない。

## 構成

- 全ページ静的生成。ビルド時に Supabase から全件取得し、実行時には叩かない
- 認証なし、フォームなし
- `/consult` の選択状態と商品詳細のタブ状態のみクライアント側で保持する
- ページ全体を `"use client"` にしない。状態が必要な部分だけ切り出す
- 新しい依存パッケージを追加しない
- max-width 480px のモバイルファースト構成を維持する
- `lib/supabase.ts` は環境変数が欠けていればビルドを失敗させる。
  どちらが欠けているか分かるメッセージにする

## デザイン

`docs/mockup.html` を参照。:root のカスタムプロパティがデザイントークン。
ただしタブバーの設置範囲と商品詳細の店員向け表示は、モックより
この文書の記述を優先する（実機比較を経て変更した）。

### トークン

```css
--bg: #f1efe8; /* 豪州版と共通 */
--card: #ffffff;
--border: #c7c4b8; /* 旧 #D3D1C7。白カードが背景から分離しなかった */
--brand: #123f66; /* ブランドヘッダー・タブ選択・日本語帯 */
--accent: #185fa5; /* 豪州版と共通。色相を変えない */
--accent-soft: #e9f0f7;
--ink: #2b2a26;
--ink-2: #63615a; /* 旧 #6E6C64 */
--stop: #b4462f;
--stop-soft: #fbf1ee;
--stop-line: #e4c4ba;
```

`--ink-3 #9A978D` は削除した。背景の上で 2.53:1 しかなく AA 不合格だった。
グレーは2段だけにする。3段目を再導入しない。

`--accent` は豪州版と共通。**色相を変えない**（姉妹プロダクトの対を保つため）。
teal 系への変更は検討済みだが不採用。

コントラスト実測値:
白/--brand 10.8:1 ／ 白/--accent 6.5:1 ／ --ink/--bg 12.6:1 ／
--ink-2/--bg 5.2:1 ／ --ink-2/白 6.0:1 ／ --stop/--stop-soft 4.8:1

### 日本語と英語の役割

画面の目的によって主役が変わる。

| 画面の目的       | 主役       | 具体例                                                                                                                                     |
| ---------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| 選ぶ（絞り込む） | **英語**   | カテゴリ一覧の商品名は romaji が先（16px/600）。Tylenol A は豪州人が読める唯一の手がかり。日本語は黒18px/700で「次に探すもの」として添える |
| 照合する         | **日本語** | 商品詳細の商品名 34px、カテゴリ画面の LOOK FOR THIS SIGN 22px                                                                              |
| 相手に見せる     | **日本語** | 完成カード17px、店員向け表示28px                                                                                                           |

移動のためのラベルは常に英語（小さく・大文字・字間広め）。
日本語を大きく出すのは「棚と照合する」「相手に見せる」ときだけ。

### placard

太枠（2px --ink）＋帯。「誰かに見せる／照合するもの」の合図にのみ使う。

**帯の言語 = その画面を読む相手の言語。**

- 商品詳細の商品名: 帯 `MATCH THIS ON THE SHELF`（英語＝旅行者が読む）
- 商品詳細の店員向け表示: 帯 `店員の方へ`（日本語＝店員が読む）
- 完成カード: 帯 `薬剤師の方へ`（日本語＝薬剤師が読む）

### 形で用途を分ける

- ブランドヘッダー: 下端のみ 18px の角丸
- タブバー: カプセル（999px）
- カテゴリ・商品カード: 10px
- placard: 4px（角丸を弱めた店頭ラベル）
- 警告: 左に 4px の赤線、右側のみ角丸

すべてを同じ角丸カードにしない。ただしカプセル形状を装飾として多用しない。

### そのほか

- 書体はシステムフォント（-apple-system / Hiragino Sans / Noto Sans JP）。
  Google Fonts の preconnect と IBM Plex の読み込みは行わない
- タイプスケールは 11 / 13 / 15 / 20 / 34 を基本にする
- カテゴリの線画アイコンは 24px、色は --accent
- 剤形アイコン（products.form）を商品カードと placard 内に置く。
  箱の形と中身は日本語が読めなくても目で確認できるため、名前の照合を裏取りする
- カテゴリ一覧の末尾に「Can't find any of these on the shelf?」と
  Ask pharmacist への導線を置く。利用者が実際に詰まるのはここ
- 影、グラデーション、ガラス風UI、背景写真、装飾アニメーションを追加しない
- カテゴリごとに異なる色を割り当てない
- **ダークモードを入れない。** 明るい店内で可読性が落ち、
  完成カードは薬剤師が腕1本分の距離から読むため常に明るい背景が必要
- 日の丸、桜、富士山、カンガルー、国旗カラーを使わない。
  独自性は `AUSTRALIA → JAPAN`、placard、日本語と英語のスケール対比が担う

### CSS の運用

`app/globals.css` の1ファイルに集約する。CSS Modules は使わない。
葉クラス名の衝突が過去に複数回発生している（`.tag` / `.field` / `.ask` /
`.sign`）。新しいクラスを追加するときは既存の名前と衝突しないか確認し、
衝突する場合は別名にする（例: `.sign` → `.shelfsign`）。

クライアントコンポーネント側で独自にスタイルを持つ場合、
`globals.css` の子孫セレクタ（`.shell .placard .band` など）が
効かないことがある。そのファイル内で完結させる。

## アクセシビリティ

- タップ可能領域は原則 44px 以上
- 360px 幅でも横スクロールを発生させない
- 重要な説明文は最低 12px、通常本文は 13〜16px
- キーボード操作で明確な `:focus-visible` を表示する
- 矢印だけの戻るリンクとボタンに `aria-label` を付ける。
  **可視テキストがある要素には付けない**（読み上げが上書きされ情報が減る）
- 複数選択に `aria-pressed`、単一選択も支援技術に選択状態が伝わるようにする
- 第1類・受診勧奨・disabled・選択状態を色だけに依存させない
  （第1類には ▲ を併記する）
- hover だけで情報を伝えない
- `prefers-reduced-motion` を妨げる動きを追加しない

## SEO

Next.js App Router の標準機能のみ。新しい依存を追加しない。
サイトURL: `https://japan-medicine-guide-omega.vercel.app`

実装済み: `metadataBase` / 共通 title と description / title template /
canonical / Open Graph / Twitter card / カテゴリごとの title と description /
商品ごとの title と description / `app/robots.ts` / `app/sitemap.ts`

sitemap に含める: `/` `/about` `/consult` 全カテゴリ 全商品
含めない: `/review`

新しい SEO 記事、ブログ、FAQ ページを追加しない。

## 監修体制

妻（Nao）が薬剤師。事実確認は妻、英文の読みやすさは私が担当。
Excel の監修シートで実施し、`/review` で `updated_at > reviewed_at` の差分を確認する。
承認ボタンやワークフローは作らない。医療の個別判断は AI にさせない。

## 収益化

無料公開を維持する。受け付けるのは一回限りの少額支援のみ。

- 広告、アフィリエイト、課金機能、サブスクリプションを入れない
- 支援の有無で表示内容を変えない。支援済み判定の仕組みを作らない
- 「Donate」ではなく「Support」。税控除対象の寄付には見せない

### 設置場所

支援導線を置くのは2か所だけ。

1. `/about` の末尾
2. 共通フッター（**ただし `/products/[slug]` を除く**）

商品詳細を除外するのは、棚の前で成分と区分を確認している最中の画面だから。
医療情報の判断中に金銭導線を近づけない。
フッターの免責文と出典表示はこのページでも従来どおり残す。
`/consult` の完成カードはフッター自体が非表示のため自動的に除外される。

カテゴリ一覧の商品カード周辺、その他あらゆる商品情報の近くには置かない。

### 実装

支援先: `https://ko-fi.com/japanmedicineguide`

- **Ko-fi の埋め込みウィジェット（iframe / script）を使わない。**
  新しい依存を増やさないため、全ページ静的生成にサードパーティ JS を
  持ち込まないため、そして金銭導線を自サイト内に描画しないため。
  通常の外部リンクにし、属性は「View manufacturer source」と揃える
- 支援導線に **--accent の塗りボタンを使わない**。
  塗りボタンは Ask the pharmacist という主要操作に予約されている
- 赤（--stop）を使わない
- Ko-fi 公式バッジ画像、新しい色トークン、影を追加しない
- DB を参照しない。新しいルートを追加しない

### 決済の前提（2026-08-31 時点で確認）

- Ko-fi は Settings > Payment のトグルで手数料が変わる。
  **新規アカウントは 5% がかかる状態で開始する。**オフにすると
  一回限りの tip は 0%（それ以外の機能は常に 5%）
- 入金は自分の Stripe アカウントに直接入る。豪州は国内カード
  1.7% + A$0.30、海外カード 3.5% + A$0.30、通貨変換 +2%（GST 込み）
- 通貨は AUD、単位は A$5。A$3 だと固定手数料の比率が約12%になる
- テーマカラーと "coffee" の比喩の変更は 5% モードでしか解放されない。
  見た目のために tip の 5% を払う根拠はまだない

### Stripe Payment Links への移行条件

Ko-fi Free は 0% で入金先も同じ Stripe のため、**コスト差はない。**
決済が外部ドメインで完結することは、医療情報と金銭導線を離すという
方針にとって欠点ではなく利点である。

移行するのは、支援が継続的に月 A$100 を超え、支援者への報告を
自前で行う必要が生じたときのみ。**ブランドの見た目を理由に移行しない。**

## 未確定・要判断

- **剤形アイコンの種類**: 32点の `form` の値を洗い出し、使うものだけ描く
- **`LOOK FOR THIS SIGN`**: 店により棚の区画表示が「かぜ薬 / 風邪薬」等と揺れる。
  `categories.name_ja` と一致しないため文言を調整するか、外す
- **カテゴリの並び順の根拠**: 公開後のタップデータで決めて記録する
- **商品詳細のタブの一貫性**: 他画面と行き先の性質が違う。
  公開後のデータで混乱が起きていないか確認する
- **「箱を持っているが名前が分からない」ケース**: v1 の失敗は検索を入口の主役に
  したことで、職務自体は残っている。公開後の離脱率を見てから判断する
- **`--border` の濃さ**: `#C7C4B8` は控えめ側。`#C2BFB2` 前後まで濃くする選択肢もある
- **foreign_brands.verified が全件 false**: 次の監修サイクルで Nao に確認する
- **`.hero` など未使用CSS**: 全画面が一巡した段階でまとめて掃除する

## 確認のしかた

全ページ静的生成のため、**ライブサイトの内容は「最後にビルドした時点のDB」**。
実装の確認は `npm run build` と `npm run start` をローカルで行い、
ライブでの確認は再デプロイ後にすること。
ライブが古く見えるときは、まずデプロイの新しさを疑う。

- **支援導線の効果**: 未検証。公開後にフッターと /about のどちらが
  押されているかを見る。押されないこと自体は失敗ではない

## 実装順

指示された画面以外は作らないこと。
無関係な変更をしないこと。未コミットファイルに触れないこと。

共通フッターの免責文は全ページに出す（旧: ホームのみ）。
`app/page.tsx` の重複ブロックは削除済み。
